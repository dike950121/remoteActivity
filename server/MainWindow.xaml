<Window x:Class="server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:server"
        mc:Ignorable="d"
        Title="TCP Server" Height="600" Width="800"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="TCP Server" FontSize="24" FontWeight="Bold" 
                       Foreground="#2C3E50" HorizontalAlignment="Center"/>
            <TextBlock Text="Simple TCP server for bot communication" 
                       FontSize="12" Foreground="#7F8C8D" 
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Server Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Port:" VerticalAlignment="Center" 
                       FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBox x:Name="PortTextBox" Grid.Column="1" Text="5555" 
                     VerticalAlignment="Center" Padding="5,3"/>
            
            <Button x:Name="StartStopButton" Grid.Column="2" Content="Start Server" 
                    Width="100" Height="30" Margin="20,0,0,0" 
                    Background="#27AE60" Foreground="White" 
                    BorderThickness="0" FontWeight="SemiBold"/>
            
            <TextBlock x:Name="StatusText" Grid.Column="3" Text="Status: Stopped" 
                       VerticalAlignment="Center" Margin="20,0,0,0" 
                       FontWeight="SemiBold" Foreground="#E74C3C"/>
        </Grid>
        
        <!-- Log Area -->
        <Border Grid.Row="2" Background="White" BorderBrush="#BDC3C7" 
                BorderThickness="1" CornerRadius="5" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="#ECF0F1" BorderBrush="#BDC3C7" 
                        BorderThickness="0,0,0,1" Padding="10,5">
                    <TextBlock Text="Server Log" FontWeight="SemiBold" 
                               Foreground="#2C3E50"/>
                </Border>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="LogTextBox" IsReadOnly="True" 
                             Background="Transparent" BorderThickness="0" 
                             Padding="10" FontFamily="Consolas" FontSize="11"
                             TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ClearLogButton" Content="Clear Log" Width="80" Height="25" 
                    Background="#95A5A6" Foreground="White" BorderThickness="0" 
                    Margin="0,0,10,0"/>
            <Button x:Name="CopyLogButton" Content="Copy Log" Width="80" Height="25" 
                    Background="#3498DB" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
